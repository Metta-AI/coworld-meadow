[package]
name = "meadow-rs"
version = "0.1.0"
edition = "2021"
description = "Zero-dependency Rust port of the Meadow commons-game simulation core"
license-file = "../LICENSE"

[dependencies]

[profile.release]
lto = true
